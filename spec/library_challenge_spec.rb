require './lib/library_challenge.rb'

describe Library do

   # let[:] { instance_double("") }

    it "has a list of books" do
    expect(subject.book_list).to eq 'data.yml'
    end

    it "book lists available books" do
        expect(subjet.)
end
